services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: new-portfolio:prod
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
